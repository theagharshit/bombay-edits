#!/usr/bin/env sh
COMMIT_HASH=$(git rev-parse --short HEAD 2>/dev/null || echo "latest")
COMMIT_MSG=$(git log -1 --pretty=%B 2>/dev/null | head -n 1)

echo " Commit succeeded: [${COMMIT_HASH}] ${COMMIT_MSG}"
echo " All code formatted, linted, and typechecked automatically."
